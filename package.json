{
  "name": "t-sub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private one-time mihomo config handoff tool for Cloudflare Workers.",
  "scripts": {
    "cf:setup": "node scripts/cloudflare-setup.mjs",
    "dev": "wrangler dev",
    "predeploy": "node scripts/check-deploy-ready.mjs",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "hash-password": "node scripts/hash-password.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

# Generate with: npm run hash-password -- "your owner password"
OWNER_PASSWORD_HASH="sha256:REPLACE_WITH_HASH_FROM_NPM_RUN_HASH_PASSWORD"

# Generate with: openssl rand -hex 32
SESSION_SECRET="replace-with-at-least-32-random-characters"

# Local default. Use your Worker or custom domain in production.
PUBLIC_BASE_URL="http://127.0.0.1:8787"
